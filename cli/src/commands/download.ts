const download = async (filename : string, dest : string) => {
  console.log(`Downloading file ${filename} to ${dest}...`);
  // Implementation of file download logic goes here
}
export default download;